# AdSense「有用性の低いコンテンツ」対策 — Claude Code 作業指示書

作成日: 2026-06-10 / 対象リポジトリ: `devtools-japan-complete`（本リポジトリ）

このファイルを読み込み、上から順に実行すること。1ツールずつ「増強 → 検証 → コミット」を回す。

---

## 0. 背景と目的

- devtools-japan.com が AdSense 審査で「有用性の低いコンテンツ（Low value content）」により複数回不承認。
- 原因は明確: **全70ツール中65本が可視テキスト1000字未満（中央値633字）**。Googleから「短い説明文だけの薄いページが大量にある」と判定されている。
- 目的: 各ツールページに**オリジナルで具体的な読み物コンテンツ**を追加し、薄さを解消する。手本は `tool-25-speech-time/index.html`（633字→2049字に増強済み。これが品質基準）。

---

## 1. 絶対遵守のガードレール（UX死守・最優先）

> ユーザー離れを起こす変更は一切禁止。違反したら作業failとみなす。

1. **既存のインタラクティブUI（`<header>`〜入力欄・ボタン・結果表示・`<script>`の計算ロジック）には一切手を入れない。** 差分は「追加のみ」。既存行の削除・改変は禁止（メタタグとJSON-LDの追記を除く）。
2. **追加コンテンツは必ずツール操作部の“下”**（既存のSEOコンテンツ領域 or その直後）に置く。ファーストビュー＝ツールが即使える状態を維持する。
3. **新規CSSは追加のみ**。既存のCSS変数・クラス（`--accent` `--border` `.seo-content` `.breakdown` 等）を再利用する。既存ルールの書き換え禁止。
4. **デザイン統一**（CLAUDE.md準拠）: 絵文字禁止 / アイコンはベタ塗りシルエットSVG（`fill="currentColor"`）/ Noto Sans JP + JetBrains Mono / 白基調 / 全処理ブラウザ完結・サーバー送信なし。
5. **モバイル幅(<600px)で崩れないこと**。横長の表は `overflow-x:auto` のラッパに入れる。
6. 追加する**数値・計算表は必ずコード（python等）で計算してから記載**。憶測値・概算の手打ち禁止（ハルシネ防止）。
7. **FAQは「見える本文」と「FAQPage JSON-LD」を必ず一致**させる。
8. 不明点・仕様の判断に迷う箇所は勝手に進めず、コメントで `# TODO要確認:` を残す（憶測禁止）。

---

## 2. 各ツールに追加する「型」（tool-25と同じ構成）

操作部の下に、`<div class="seo-content">` を使って以下を追加（既存に同セクションがあれば中身を拡充）:

1. **使い方・具体例**: そのツールならではの実例ベース解説（一般論の水増し禁止。独自の切り口・具体的な数字や場面を入れる）。
2. **早見表 / 対応表**: そのツール特有の参照データ（下記ブリーフ参照）。`<table class="lookup-table">`。数値はコードで計算して検証。
3. **よくある質問（3〜5問）**: 見える本文（`.faq-item`）＋ FAQPage JSON-LD の両方に同内容を入れる。検索クエリ（GSC実データ）に寄せた質問にする。
4. （任意）背景知識・注意点。

**1ページの目標: 可視テキスト 1,000〜1,500字以上。** `tool-25-speech-time/index.html` の構造をそのまま流用してよい（`.lookup-table` `.faq-item` のCSSも同様に追加）。

---

## 3. 優先順リストと個別コンテンツブリーフ

GSCで検索表示のある流入ページを、薄い順に増強する。**この順番で実施**。

### ① tool-29-byte-converter（現514字 / クエリ「バイト 変換」）
- 早見表: B / KB / MB / GB / TB の相互換算表（1024進）。代表値（1KB, 1MB, 100MB, 1GB, 1TB 等）を一覧化。
- 解説: 2進（1024）と10進（1000, GiB/GB）の違い。OS表示とストレージ容量がズレる理由。
- FAQ例:「1GBは何MB？」「KBとKiBの違いは？」「1TBは何GB？」

### ② tool-22-withholding-tax（現592字 / 源泉徴収）
- 計算式の明示: 報酬10.21%（100万円超部分20.42%）。
- 早見表: 報酬額別（1万〜50万）の源泉徴収額・手取り早見表（**python計算で検証**）。
- 解説: 原稿料・講演料・デザイン料など対象例。請求書での書き方。
- FAQ例:「10万円の報酬の源泉徴収は？」「消費税込みでも源泉は引かれる？」

### ③ tool-28-aspect-ratio（現593字）
- 早見表: 主要比率（16:9, 4:3, 1:1, 9:16, 21:9, 3:2）× 代表解像度の対応表。
- 解説: 用途別（YouTube=16:9, Instagram=1:1/4:5, TikTok/Reels=9:16, 印刷=3:2）。
- FAQ例:「16:9とは？」「YouTubeのサムネ比率は？」

### ④ tool-53-age-calc（現738字）
- 早見表: 西暦⇔和暦＋年齢の対応（直近の代表生年）。節目（成人18歳, 還暦60, 古希70 等）。
- 解説: 年齢の数え方（満年齢）、勤続年数の数え方。
- FAQ例:「勤続年数は入社日から今日まで？」「数え年と満年齢の違いは？」

### ⑤ tool-64-take-home-pay（現789字）
- 早見表: 年収別（300万〜1000万）の手取り目安表（**python計算で検証**。社保・税の概算前提を明記）。
- 解説: 額面と手取りの差の内訳（健康保険・厚生年金・雇用保険・所得税・住民税）。
- FAQ例:「年収500万の手取りは？」「ボーナスの手取りは？」
- ⚠️ 概算であること・前提条件を必ず明記（断定回避）。

### ⑥ tool-57-stamp-duty（現948字）
- 早見表: 契約書・領収書の記載金額帯別 印紙税額一覧（第1号・第2号文書、領収書=第17号）。**国税庁の区分に厳密に従う**。出典URL（国税庁）を明記。
- 解説: 電子契約は印紙不要、非課税となるケース。
- FAQ例:「5万円未満の領収書は？」「電子契約に印紙は必要？」
- ⚠️ 税額は国税庁の公式区分を確認してから記載（`# TODO要確認:` で出典を残す）。

### ⑦ tool-23-sns-image-sizes（現1125字 / クエリ「twitter 画像 サイズ」「note 見出し画像 推奨サイズ 2026」）
- 既に厚め。**note見出し画像**セクションを追加（クエリ実需あり）。各SNS最新サイズの根拠・更新日を明記。
- FAQ例:「noteの見出し画像の推奨サイズは？」「Xのヘッダー画像サイズは？」

### ⑧ tool-21-tax-simulator（現1234字 / クエリ「副業 税金シュミレーション」）
- 既に厚め。軽微でよい。FAQ追加（「副業の税金はいくらから？」「住民税・国保の扱いは？」）。表記ゆれ「シュミレーション」も本文/FAQに自然に含める。

---

## 4. その他のAdSense対策（UX非劣化を前提に対応）

1. **ドメイン上に独自解説記事 `blog/` を5〜10本**（各1,500字以上, オリジナル）。Qiita/Zennの記事は“外部”で審査対象外のため、自ドメインに編集コンテンツを置くことが重要。候補タイトル:
   - 「源泉徴収の計算方法を実例で解説（報酬・原稿料）」（→tool-22へ内部リンク）
   - 「インボイス制度の消費税計算 完全ガイド」（→tool-68/65）
   - 「プレゼン・YouTube台本の文字数と時間の決め方」（→tool-25）
   - 「契約書の収入印紙はいくら？文書別early一覧」（→tool-57）
   - 「年収から手取りを正しく見積もる方法」（→tool-64）
   - 既存 `blog/sns-image-size-guide` の体裁・デザインに合わせる。
2. **E-E-A-T 強化**: `/about/` に運営者情報・専門性・運営方針を明記。各解説記事に著者表記と更新日。`/privacy/` `/contact/` は既存を維持（リンク健全性を確認）。
3. **内部リンク**: 各ツール ↔ 関連解説記事 を相互リンク（既存「関連ツール」ブロックの様式を踏襲）。
4. **超薄い無流入ツールの処理**: 下記は流入ゼロかつ極端に薄い。**「②の型で増強」か「再審査の間だけ `<meta name="robots" content="noindex">` を付与」のどちらか**。noindexはユーザーは普通に使えて検索除外only＝UX非劣化。薄ページの枚数を減らし平均品質を上げる狙い。
   - tool-41-text-reverse(253) / tool-50-htaccess-gen(284) / tool-45-chmod-calc(306) / tool-34-text-counter-detail(365) / tool-48-jwt-decoder(384) / tool-43-word-counter-en(399) / tool-32-unit-converter(447) / tool-33-emoji-search(472) / tool-40-placeholder-img(476) / tool-47-html-color-names(479) / tool-46-color-palette(490)
   - 判断に迷うものは増強を優先（noindexはSEO流入を捨てるため最小限に）。
5. **sitemap.xml 更新 → Search Console再送信**。増強・noindex後にインデックス状況を確認。
6. **再申請は実質改修が一通り終わってから、前回不承認の2〜3週間後**。短期の連続再申請は同じ理由で落ちるので禁止。

---

## 5. 1ツールごとの作業手順（チェックリスト）

各ツールで以下を順守:

1. 対象 `index.html` と `tool-25-speech-time/index.html`（手本）を読む。
2. 必要な数値表を **python等で計算**して値を確定（コードも残す）。
3. `<style>` に `.lookup-table` `.faq-item` のCSSが無ければ追加（tool-25からコピー、既存ルールは触らない）。
4. 操作部の下に `.seo-content` ブロックで「使い方・具体例 / 早見表 / FAQ」を追加。
5. FAQPage JSON-LD に同じQ&Aを追記（既存FAQPchがあれば mainEntity に追加、無ければ新規 script を追加）。
6. title / meta description を軽く調整（クエリ語を自然に含める。UIではないので可）。既存の主要キーワードは残す。
7. 下記「検証」を全項目パスさせる。
8. `git add <そのファイル>` → コミット（例: `Thicken tool-29 content (byte conversion table + FAQ) for AdSense`）。

---

## 6. 検証（各ツールでコミット前に必須実行）

```python
import re,json,glob
f="tool-XX-xxxx/index.html"   # 対象を指定
html=open(f,encoding='utf-8').read()
# 1) div開閉バランス
o=len(re.findall(r'<div\b',html)); c=len(re.findall(r'</div>',html))
assert o==c, f"div不一致 {o}/{c}"
# 2) ld+json 全て妥当 & FAQPageの件数
for b in re.findall(r'application/ld\+json">(.*?)</script>',html,re.S):
    d=json.loads(b)  # 例外が出たらJSON破損
# 3) 既存UI/JSが無傷か（ツール固有の主要トークンで確認）
for tok in ['<script>','</script>']:
    assert tok in html
# 4) 可視テキスト量
b=re.sub(r'<script.*?</script>','',html,flags=re.S); b=re.sub(r'<style.*?</style>','',b,flags=re.S)
t=re.sub(r'<[^>]+>',' ',b); chars=len(re.sub(r'\s','',t))
assert chars>=1000, f"まだ薄い: {chars}字"
print("OK", chars, "字")
```

- 表に数値がある場合は、表のセル値を**計算式で再計算して一致を確認**（tool-25のチェック手法を踏襲）。
- 手元でブラウザ表示し、(a)ツールが従来通り動く (b)モバイル幅で崩れない、を目視確認。

---

## 7. デプロイ

```bash
git add <対象ファイル>
git commit -m "Thicken <tool> content for AdSense (UX unchanged)"
git push        # SSH認証済み。Vercelが自動デプロイ
```

- まとめてではなく**1〜数ツールごとにpush**して、万一の表示崩れを小さく抑える。
- 全バッチ完了 + sitemap再送信まで終えてから、AdSense再申請（前回から2〜3週間以上空ける）。

---

## 8. 完了の定義

- 優先8ツールが各1,000字以上＋FAQ＋（該当すれば）早見表を持つ。
- 解説記事 `blog/` が最低5本（各1,500字以上）。
- 超薄い無流入ツールが「増強済み or noindex」。
- 全ページで検証スクリプトがパス、UI/UX非劣化を目視確認済み。
- sitemap更新・GSC再送信済み。
