# 新ツール追加（6本）— Claude Code 作業指示書（第4弾）

作成日: 2026-06-16 / 対象リポジトリ: `devtools-japan-complete`

## 目的
検索クエリの未対応needを埋める新ツールを6本追加する。**新規も初日から品質基準（1,000字以上＋FAQ＋早見表＋SEO一式）で作る**。薄いまま公開は禁止。**過去にバグ（白文字残骸・置換未反映・FAQ重複・数値誤り）が多発したため、本書はQAゲートを大幅強化している（§5必読）。**

## 追加するツール（slug・番号）
| No | slug | 内容 | 根拠クエリ |
|---|---|---|---|
| tool-71 | tool-71-car-depreciation | 車（自動車）減価償却 計算 | 「車 減価償却費 自動計算」34表示 |
| tool-72 | tool-72-bonus-take-home | ボーナス（賞与）手取り計算 | 「ボーナス 手取り」需要大 |
| tool-73 | tool-73-retirement-tax | 退職金 手取り・税金計算 | 「退職金 手取り/税金」需要大 |
| tool-74 | tool-74-social-insurance | 社会保険料 計算 | 「社会保険料 計算」需要大 |
| tool-75 | tool-75-residence-tax | 住民税 計算 | 「住民税 計算/いくら」需要大 |
| tool-76 | tool-76-manuscript-paper | 原稿用紙 換算（文字数⇔枚数） | 最大流入クラスタ(読み上げ時間)隣接 |

---

## 1. 設計・デザイン（厳守）
- **手本**: 既存の完成度が高いツールを雛形にする。お金系は `tool-64-take-home-pay/index.html`、文字数系は `tool-25-speech-time/index.html` の構造・CSS・トークンをそのまま流用。
- 白基調テーマ（`--bg:#f7f8fb` `--bg-card:#ffffff` `--text:#1e1e35` 等）。**ダークテーマ残骸を持ち込まない**：`color:#fff`等の白系文字を白背景で使わない／`var(--bg-elevated,#0c0c14)`等の暗いフォールバックを使わない／back-linkホバーは`#1e1e35`。
- 絵文字禁止、アイコンはベタ塗りSVG（`fill="currentColor"`）、Noto Sans JP + JetBrains Mono、**全処理ブラウザ完結・サーバー送信なし**。
- gtag(G-E27Q8YTG7L)・AdSense(ca-pub-7300747004702072)タグ、canonical、OGP/Twitter、JSON-LD(WebApplication+BreadcrumbList+FAQPage)を既存ツール同様に完備。
- モバイル幅(<600px)で崩れない（横長表は`.table-wrap`で`overflow-x:auto`）。

## 2. コンテンツ（各ツール1,000字以上）
操作部の下に `.seo-content` で「使い方・具体例／早見表／よくある質問5問」。FAQは**見える本文とFAQPage JSON-LDを完全一致（5問）**。

## 3. 計算ロジックと正確性（最重要・憶測禁止）
- **税・制度の数値は必ず公式（国税庁・総務省・協会けんぽ・財務省）に準拠**。2026年（令和8年度）税制改正後の値を使う。**確証が持てない値（料率・控除額・耐用年数・改正の適用時期）は実装に使わず `// TODO要確認:` ＋出典URLを残し、人間に確認を仰ぐ**。
- 各ツールの計算要件:
  - **tool-71 車減価償却**: 法定耐用年数（普通車6年・軽4年・※貨物等は要確認）、新車/中古（中古は簡便法：(法定耐用年数−経過年数)+経過年数×20%、最低2年）、定額法/定率法、事業利用割合。出典=国税庁タックスアンサー(耐用年数表)。
  - **tool-72 ボーナス手取り**: 標準賞与額×社会保険料率（健康保険・厚生年金18.3%・雇用保険、労使折半）＋賞与の源泉所得税（賞与に対する源泉徴収税額の算出率表）。協会けんぽ料率は概算であること・地域差を明記。
  - **tool-73 退職金**: 退職所得控除（勤続20年以下=40万×年・最低80万、20年超=800万+70万×(年-20)）、課税退職所得=(退職金−控除)×1/2、所得税(分離・速算表)＋住民税10%。出典=国税庁。
  - **tool-74 社会保険料**: 標準報酬月額ベースで健康保険・介護(40〜64歳)・厚生年金18.3%・雇用保険、労使折半の本人負担を表示。協会けんぽ料率(概算)。
  - **tool-75 住民税**: 所得割10%(市区町村6%+都道府県4%)＋均等割＋調整控除。基礎控除等は2026改正の住民税側の値・適用年度に注意（**住民税は2027年度〜適用の項目あり。要確認・出典必須**）。
  - **tool-76 原稿用紙換算**: 文字数⇔400字詰め枚数、字数→ページ数(レポート等の任意字数/頁設定)。計算は単純（公的数値不要）。
- **表・結果の数値は、そのツールのJS計算式と同一ロジックで算出**し整合させる。

## 4. サイトへの登録（忘れず実施）
- `index.html`（トップ）のツール一覧に6件追加、関連ツールの内部リンク設定、**ツール数表記を更新**（現「70」系→「76」。`全70ツール`バッジ・`70+`・`70種類以上`等を一括見直し）。
- `sitemap.xml` に6URL追加＋lastmod更新。
- 各新ツールに「関連ツール」ブロック（既存様式・白基調）を設置。

---

## 5. 品質ゲート（バグ多発対策・各ツールでコミット前に全項目パス必須）

> 過去の事故：①置換が実ファイルに反映されていなかった ②白文字が白地で不可視 ③FAQPage重複 ④数値誤り。以下で全て潰す。

**G1. 編集の実反映確認**: 編集後、対象ファイルを**必ず読み直し**、意図した内容が実在することを grep で確認（ハーネスのパース不具合対策）。

**G2. 計算の独立再検証**: ツールのJS計算式とは**別に**、同じ入力で期待値をPython等で再計算し一致をassert。境界値（中古2年下限、勤続20年境界、40歳介護保険、均等割等）も検証。公式の計算例があれば突き合わせる。

**G3. 静的検証スクリプト**:
```python
import re,json
f="tool-XX-.../index.html"; h=open(f,encoding='utf-8').read()
assert len(re.findall(r'<div\b',h))==len(re.findall(r'</div>',h)),"div不一致"
faq=[json.loads(b) for b in re.findall(r'application/ld\+json">(.*?)</script>',h,re.S)]  # 例外=JSON破損
fp=[d for d in faq if d.get('@type')=='FAQPage']; assert len(fp)==1,"FAQPageは1個"
vis=len(re.sub(r'\s','',re.sub(r'<[^>]+>',' ',re.sub(r'<(script|style).*?</\\1>','',h,flags=re.S))))
assert vis>=1000,f"薄い:{vis}"
assert re.findall(r'class="faq-item"',h).__len__()==len(fp[0]['mainEntity']),"FAQ本文とJSON-LD不一致"
print("G3 OK",vis)
```

**G4. 色/UX監査（ダークテーマ残骸の混入防止）**:
```bash
# 白系テキストが暗い背景なしで使われていないか／暗いフォールバックが無いか
grep -nE "color:#(fff|ffffff|f0f|e0e|e4e4f0|d9d9d9)" tool-XX-.../index.html
grep -n "var(--bg-elevated,#0c0c14)\|this.style.color='#e4e4f0'" tool-XX-.../index.html
# いずれも該当行が出たら（accent背景上のボタン等の正当な白文字を除き）修正
```

**G5. ブラウザ実機確認（必須）**: 各ツールをブラウザで開き、(a)代表入力で計算結果がG2の期待値と一致 (b)コンソールエラーなし (c)白地に白文字や不可視要素がない (d)モバイル幅で崩れない (e)既存ツールと見た目が統一、を目視確認。

**G6. 全体回帰**: トップのツール数表記が全箇所「76」で統一されているか、sitemapがXMLとして妥当か、既存ツールに影響が出ていないかを確認。

---

## 6. デプロイとタイミング
```bash
git add tool-71-* tool-72-* tool-73-* tool-74-* tool-75-* tool-76-* index.html sitemap.xml
git commit -m "Add 6 new tools (car depreciation, bonus/retirement/social-insurance/residence tax, manuscript paper)"
git push   # SSH・Vercel自動
```
- 1〜数本ごとにpush。
- **AdSense再申請の直前（約10日以内）に新ページを大量投入しない**。再申請の10日以上前に出してインデックスさせる、または承認後に追加（再評価期の事故防止）。

## 7. 完了の定義
- 6ツールが各1,000字以上＋FAQ5＋（該当すれば）早見表を持ち、G1〜G6を全てパス。
- 税・制度の数値は公式準拠・出典明記、確証なき値は`// TODO要確認:`で人間判断待ち。
- トップのツール数「76」統一、sitemap更新、UI/UX統一・無改変（既存）。
